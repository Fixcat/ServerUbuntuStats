# 🔧 Быстрое исправление установки

Если после установки команда `stats` не работает с ошибкой "cannot execute: required file not found", выполните:

## Вариант 1: Быстрое исправление (одна команда)

```bash
sudo sed -i 's/\r$//' /usr/local/bin/stats && sudo chmod +x /usr/local/bin/stats && stats
```

## Вариант 2: Переустановка

```bash
cd ServerUbuntuStats
sudo sed 's/\r$//' stats > /usr/local/bin/stats
sudo chmod +x /usr/local/bin/stats
stats
```

## Вариант 3: Запуск без установки

```bash
cd ServerUbuntuStats
sed -i 's/\r$//' stats
chmod +x stats
./stats
```

## Проверка установки

```bash
# Проверить, что файл существует
ls -la /usr/local/bin/stats

# Проверить первую строку (должно быть #!/bin/bash)
head -1 /usr/local/bin/stats

# Запустить напрямую через bash
bash /usr/local/bin/stats
```

## Причина проблемы

Проблема возникает из-за разных окончаний строк в Windows (CRLF) и Linux (LF). 
Команда `sed 's/\r$//'` конвертирует окончания строк в формат Linux.

## После исправления

Команда `stats` будет работать из любой директории!

```bash
stats
```
